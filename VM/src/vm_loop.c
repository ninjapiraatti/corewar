# include "vm.h"
 
while (/* condition */)
    {
        /* carriages read info, save next_state; */
        // wait ajat
        // kattoo, et onko cycles_to_die aika
    //if cycles_to_die
     //    check
    }
    